---
layout: null
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom"
      xmlns:dc="http://purl.org/dc/elements/1.1/"
      xmlns:media="http://search.yahoo.com/mrss/">
  
  <title>{{ site.title | xml_escape }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  <link href="{{ site.url }}/atom.xml" rel="self" type="application/atom+xml"/>
  <link href="{{ site.url }}/" rel="alternate" type="text/html"/>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ site.url }}/</id>
  
  <author>
    <name>{{ site.author | default: site.title | xml_escape }}</name>
    {% if site.email %}<email>{{ site.email | xml_escape }}</email>{% endif %}
    <uri>{{ site.url }}/</uri>
  </author>
  
  {% if site.lang %}
  <language>{{ site.lang | xml_escape }}</language>
  {% endif %}
  
  {% for post in site.posts limit:50 %}
  <entry>
    <title>{{ post.title | xml_escape }}</title>
    <link href="{{ site.url }}{{ post.url | xml_escape }}" rel="alternate" type="text/html"/>
    <id>{{ site.url }}{{ post.url | xml_escape }}</id>
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.date | date_to_xmlschema }}</updated>
    
    {% if post.author %}
    <author>
      <name>{{ post.author | xml_escape }}</name>
    </author>
    {% endif %}
    
    <!-- Categories: text only, no links -->
    {% for category in post.categories %}
    <category term="{{ category | xml_escape }}"/>
    {% endfor %}
    
    <!-- Tags: text only, no links -->
    {% for tag in post.tags %}
    <category term="{{ tag | xml_escape }}"/>
    {% endfor %}
    
    <!-- Summary (feed reader list view) - plain text -->
    <summary type="text">
      {% if post.excerpt %}
        {{ post.excerpt | strip_html | truncate: 200 }}
      {% else %}
        {{ post.content | strip_html | truncate: 200 }}
      {% endif %}...
    </summary>
    
    <!-- Full content with formatting preserved, original link at bottom -->
    <content type="html">
      <![CDATA[
        <article>
          <header>
            <h1>{{ post.title | xml_escape }}</h1>
            <p class="meta">
              Published on <time datetime="{{ post.date | date_to_xmlschema }}">{{ post.date | date: "%B %d, %Y" }}</time>
              {% if post.categories.size > 0 %}
              | Categories: {{ post.categories | join: ', ' | xml_escape }}
              {% endif %}
            </p>
          </header>
          
          {{ post.content }}
          
          {% if post.tags.size > 0 %}
          <hr>
          <p>Tags: {{ post.tags | join: ' · ' | xml_escape }}</p>
          {% endif %}
          
          <!-- Original link preserved -->
          <hr>
          <p style="color: #666; font-style: italic;">
            🔗 Original article: <a href="{{ site.url }}{{ post.url | xml_escape }}">{{ site.url }}{{ post.url | xml_escape }}</a>
          </p>
        </article>
      ]]>
    </content>
    
    <!-- Featured image (if available) -->
    {% if post.image %}
    <media:thumbnail url="{{ site.url }}{{ post.image | relative_url }}"/>
    <media:content url="{{ site.url }}{{ post.image | relative_url }}" type="image/jpeg"/>
    {% endif %}
    
  </entry>
  {% endfor %}
  
</feed>