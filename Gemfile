# Gemfile
source "https://rubygems.org"
ruby "3.3.5"  # 或者留空让 CF 默认用 3.3

gem "jekyll", "3.9.5"
gem "webrick"
gem "csv"       # 关键修复行
gem "logger"    # 关键修复行

group :jekyll_plugins do
  gem "jekyll-regex-replace"
  # 如果你还用了其他插件在这里加
end